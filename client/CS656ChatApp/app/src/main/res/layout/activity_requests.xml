<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000"
    tools:context="com.cs656chatapp.client.RequestsActivity">

    <TextView
        android:id="@+id/req_textView"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="@string/Reqs"
        android:layout_marginTop="30dp"
        android:textSize="20dp"
        android:textColor="@color/White"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="15dp"/>

    <ListView
        android:id="@+id/requestList"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:background="@drawable/customborder"
        android:layout_alignLeft="@+id/req_textView"
        android:layout_alignRight="@+id/req_textView"
        android:layout_below="@+id/req_textView"/>

    <Button
        android:id="@+id/go_back2"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignLeft="@+id/requestList"
        android:layout_alignRight="@+id/requestList"
        android:layout_below="@+id/requestList"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/buttoninputborder"
        android:text="@string/go_back"
        android:textColor="@color/White"
        android:textSize="15sp" />


</RelativeLayout>
